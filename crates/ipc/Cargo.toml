[package]
name = "ultidesk-ipc"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
ultidesk-core = { workspace = true }
ultidesk-identity = { workspace = true }
ultidesk-topology = { workspace = true }
# For the shared input shapes (`VirtualScreen`, `MouseButton`, `InputError`), which live
# in the windows crate for now by its own admission — see its module docs.
ultidesk-platform-windows = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
uuid = { workspace = true }
tokio = { version = "1", features = ["rt", "net", "io-util", "time", "macros"] }
