[package]
name = "ultidesk-topology"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
serde = { workspace = true }
ultidesk-core = { workspace = true }

[dev-dependencies]
# Only the tests build monitors with fixed device ids, so two machines can be told apart
# deterministically rather than by chance.
uuid = { workspace = true }
