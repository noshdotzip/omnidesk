# Rust
/target
**/target
Cargo.lock.orig

# Node / Electron
node_modules/
dist/
out/
.vite/
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Ultidesk local runtime state (never commit secrets/identity)
.ultidesk-dev/
*.local.json
secrets/
# The device private key, wherever a run drops it — a checkout pointed at by
# ULTIDESK_CONFIG_DIR included. Matched by file name rather than by a directory named
# `identity/`, which also swallowed the `crates/identity` source crate.
identity.json

# Generated protocol code (regenerated from protocol/ultidesk.proto)
packages/protocol-ts/src/generated/
