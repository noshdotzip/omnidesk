{
  "name": "@ultidesk/desktop",
  "private": true,
  "version": "0.0.1",
  "description": "Ultidesk desktop app (Electron): tray UI, window picker, projection proxy windows, WebRTC media, agent IPC bridge.",
  "license": "UNLICENSED",
  "main": "dist/main/main.js",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint \"src/**/*.ts\"",
    "dev": "echo \"GUI run requires the Vite bundler wiring tracked in docs/status.md (Milestone 0 follow-up).\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "22.10.5",
    "electron": "33.3.1",
    "typescript": "5.7.3",
    "vitest": "2.1.8"
  }
}
